name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - ci-investigation
  pull_request:
    paths-ignore:
      - doc/**
      - LICENSE
      - README.md

permissions:
  contents: read

jobs:
  test-on-vim:
    if: false
    name: Vim ${{ matrix.vim_version }} on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest

        vim_version:
          # - v9.1.2110
          # - v9.1.2120
          # - v9.1.2125
          - v9.1.2130
          - v9.1.2131  # vim --version shows that it is v9.1.2132
          - v9.1.2132
          # - v9.1.2133
          # - v9.1.2134
          # - v9.1.2135

        download:
          - available

    uses: mityu/workflow-run-vim-themis/.github/workflows/run-themis.yml@v20260116
    with:
      vim_type: vim
      vim_version: ${{ matrix.vim_version }}
      os: ${{ matrix.os }}
      download: ${{ matrix.download }}

  investigation:
    name: Vim ${{ matrix.vim_version }} ${{ matrix.configure_args }} on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
        vim_version:
          - nightly
        configure_args:
          - --enable-gui=no --enable-fail-if-missing
          - --enable-gui=gtk3 --enable-fail-if-missing

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - name: Setup Vim
        id: vim
        uses: rhysd/action-setup-vim@v1
        with:
          version: ${{ matrix.vim_version }}
          configure-args: ${{ matrix.configure_args }}
      - name: Show Vim version
        run: |
          ${{ steps.vim.outputs.executable }} --version
      - name: Setup themis.vim
        uses: actions/checkout@v6
        with:
          repository: thinca/vim-themis
          path: themis
      - name: Run test
        env:
          THEMIS_VIM: ${{ steps.vim.outputs.executable }}
        run: |
          ./themis/bin/themis --reporter tap
